"use client";
import { ArrowLeft, MoreHorizontal, Pencil, Trash2, Users } from "lucide-react";
import { useState } from "react";
import Modal from "@/components/common/Modal";
import BoardForm from "./BoardForm";
export default function BoardHeader({ board, isOwner, onBack, onRename, onDelete, onMembers }) { const [menu,setMenu]=useState(false);const [edit,setEdit]=useState(false);return <header className="sticky top-0 z-30 border-b border-slate-200 bg-white/95 backdrop-blur"><div className="mx-auto flex max-w-[1500px] items-center gap-3 px-4 py-3 sm:px-6"><button onClick={onBack} className="rounded-xl p-2 text-slate-500 hover:bg-slate-100"><ArrowLeft size={20}/></button><div className="min-w-0 flex-1"><h1 className="truncate text-lg font-bold text-slate-900">{board.title}</h1><p className="text-xs text-slate-500">{isOwner?"You own this board":"Shared board"}</p></div><button onClick={onMembers} className="inline-flex items-center gap-2 rounded-xl border border-slate-200 px-3 py-2 text-sm font-semibold text-slate-700 hover:bg-slate-50"><Users size={16}/><span className="hidden sm:inline">Members</span></button>{isOwner&&<div className="relative"><button onClick={()=>setMenu(v=>!v)} className="rounded-xl border border-slate-200 p-2 text-slate-500 hover:bg-slate-50"><MoreHorizontal size={18}/></button>{menu&&<div className="absolute right-0 mt-2 w-40 rounded-xl border border-slate-200 bg-white p-1 shadow-lg"><button onClick={()=>{setEdit(true);setMenu(false)}} className="flex w-full items-center gap-2 rounded-lg px-3 py-2 text-sm hover:bg-slate-50"><Pencil size={14}/> Rename</button><button onClick={onDelete} className="flex w-full items-center gap-2 rounded-lg px-3 py-2 text-sm text-red-600 hover:bg-red-50"><Trash2 size={14}/> Delete</button></div>}</div>}</div><Modal open={edit} title="Rename board" onClose={()=>setEdit(false)}><BoardForm initialTitle={board.title} onSubmit={async title=>{await onRename(title);setEdit(false)}} onCancel={()=>setEdit(false)}/></Modal></header>; }
