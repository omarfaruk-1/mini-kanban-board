"use client";
import { useState } from "react";
import toast from "react-hot-toast";
import { UserPlus, Trash2 } from "lucide-react";
import { memberService } from "@/services/member.service";
import Input from "@/components/common/Input";
import Button from "@/components/common/Button";
export default function BoardMembers({ board, isOwner, onChanged }) { const [email,setEmail]=useState("");const [loading,setLoading]=useState(false);async function add(e){e.preventDefault();setLoading(true);try{await memberService.add(board.id,email.trim());setEmail("");toast.success("Member added");await onChanged()}catch(e){toast.error(e.message)}finally{setLoading(false)}}async function remove(id){try{await memberService.remove(board.id,id);toast.success("Member removed");await onChanged()}catch(e){toast.error(e.message)}}return <div>{isOwner&&<form onSubmit={add} className="mb-6 rounded-xl bg-slate-50 p-4"><div className="flex gap-2"><div className="flex-1"><Input label="Registered user email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="teammate@example.com" type="email" required/></div><Button loading={loading} className="mt-6"><UserPlus size={16}/> Add</Button></div><p className="mt-2 text-xs text-slate-500">Only the board owner can add or remove members.</p></form>}<div className="space-y-2"><div className="flex items-center justify-between rounded-xl border border-indigo-100 bg-indigo-50 p-3"><div><p className="text-sm font-semibold text-slate-800">{board.owner?.name}</p><p className="text-xs text-slate-500">{board.owner?.email}</p></div><span className="rounded-full bg-white px-2.5 py-1 text-[11px] font-semibold text-indigo-700">Owner</span></div>{board.members?.map(m=><div key={m.id} className="flex items-center justify-between rounded-xl border border-slate-200 p-3"><div><p className="text-sm font-semibold text-slate-800">{m.user.name}</p><p className="text-xs text-slate-500">{m.user.email}</p></div>{isOwner&&<button onClick={()=>remove(m.userId)} className="rounded-lg p-2 text-slate-400 hover:bg-red-50 hover:text-red-600"><Trash2 size={16}/></button>}</div>)}</div></div>; }
